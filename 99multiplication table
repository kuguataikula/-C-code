#include <stdio.h>
void multiplication();
int main() {
	multiplication();
	
	return 0;
}
void multiplication() {
	for (int i = 1; i <= 9; i++) {
		for (int j = 1; j <= i; j++) {
			printf("%d x %d= %d", i, j, i * j);
			if (i==j) {
				printf("\n");
			}
		}
	}
}
