#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>

int main()
{
	for (int a=0; a < 5; a++) {
		int numb = 0;
		for (int b=0; b < 5; b++) {
			for (intc=0; c < 5; c++) {
				numb = a * 100 + b * 10 + c;
				printf("%d\n", numb);
			}
		}
	}
	return 0;
}
