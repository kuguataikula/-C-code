int main() {
	double a = 0.0f, b = 0.0f, c = 0.0f;
	scanf("%lf %lf %lf", &a, &b, &c);
	double m = (a + b + c) / 2;
	double n = sqrt(m * (m - a) * (m - b) * (m - c));
	printf("%.2lf", n);
	return 0;
}
